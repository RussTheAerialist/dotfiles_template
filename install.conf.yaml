- clean: ['~']

- link: 
    ~/.vimrc: vimrc
    ~/.zshrc: zshrc
    ~/.taskrc: taskrc
    ~/.oh-my-zsh: oh-my-zsh/
    ~/.vim: vim/

- shell:
  - [git submodule update --init --recursive, Installing/updating submodules]
